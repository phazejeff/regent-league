[project]
name = "regent-league-api"
version = "0.1.0"
description = "API for Regent League"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "requests>=2.32.5",
    "sqlmodel>=0.0.24",
]
